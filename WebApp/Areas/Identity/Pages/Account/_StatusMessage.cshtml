@model string

@{
    // Account area delegates to the shared status message partial so auth pages keep the same behavior without duplicating markup.
}

<partial name="~/Views/Shared/_StatusMessage.cshtml" model="Model" />

namespace WebApp.Services.ExcelImport;

// Defines stable identifiers for Excel import jobs in the shared background job runtime.
public static class ExcelImportBackgroundJobConstants
{
    public const string ExecuteJobType = "excelimport.execute";
}
